# 0.1.0 验收记录

## 0.1.2 · 恢复阅读层与预览联动

- 模块点击／展开、编辑、开关、恢复建议与结构地图操作会切回图文，并滚动到对应卡片；关闭后定位原文锚点。
- 重点表达操作定位原句；高亮提示只用于页面预览，不进入导出的公众号正文。
- 浏览器实测覆盖音频页切回图文、目标卡片进入右侧可视区、关闭后原文定位、重开与编辑后联动，真实剪贴板不带定位属性或提示样式。
- 用户正在查看的已有页面只更新界面脚本和样式，内嵌课程数据及构建标识保持原样，避免丢失浏览器草稿。

## 0.1.1 · 恢复原版可见入口

- 按原课程工作台恢复顶部“导入课程包／导出本课配置／复制到公众号”名称。
- 公众号文章封面、播客音频封面独立常显；两者均支持真实上传、放大与下载，播客封面与播放器同步。
- 恢复可见的本期发布交付包、转写校对稿下载、标题／副标题复制与图文／播客播放／播客原文三态预览。
- 恢复原版深色顶栏与预览底色、中栏优先的三栏比例。原稿与正文渲染逻辑未改动。
- 浏览器回归新增原按钮名称、封面不在折叠区、双封面实际文件上传／下载／放大与播放器联动检查；既有单元测试和公众号校验继续通过。

## 首版验收

2026-09-10，本地实现验收；未执行公众号平台上传或发布。

- `quick_validate.py`：技能格式通过；安装副本同样通过。
- `npm test`：10 项通过。包括长稿不截断、原稿变更识别、重点唯一命中与重叠、输入转义、危险素材路径、包往返、非空输出保护、原粗体与代码保护。
- `npm run test:browser`：独立 Chrome 实测通过。包括真实剪贴板 HTML、刷新恢复、配置文件下载／重新导入、非法输入保留原状态、链接校验、导入另一课后恢复、存储满时提示、移动端无横向溢出和页面无 JavaScript 错误。
- 私有原有课程夹具：7 课构建通过；304 行正文逐行比较通过（Markdown 展示标记去除、中文排印规范化后比较，原稿文件与 JSON 原文保持原样）；音频元数据和两类封面加载通过。真实夹具与截图不随仓库发布。
- 7 课正文通过本机已有 gzh-design 校验器：每课 0 ERROR / 0 WARNING。工具不携带或复制该外部校验器。
- 公开可复用的虚构示例通过校验，明确提示音频、图文封面和播客封面待补，没有伪造媒体素材。

## 验收的边界

技能版沿用课程 1.0 配置与三栏阅读层工作方式，提取了独立可迁移的渲染与编辑实现；不是旧单文件工作台的逐像素复制。KW 与原 Muse 文件均未修改。网页结果需要在公众号后台检查实际粘贴和手机预览，不能拿结构校验替代平台验收。

浏览器草稿只在当前浏览器可恢复；跨设备使用导出的配置与整个发布文件夹。目录中 `article.html` 是构建快照，网页修改后应下载当前正文，或导出配置后重新构建。

## 0.1.3 · 系统一致性排查

详见 [AUDIT-PARITY.md](AUDIT-PARITY.md)。12 项 Node 测试、完整浏览器回归、七课 304 行原文与素材加载检查通过。补齐恢复基准、发布确认、地图入口、听音跳转、播客内容与章节校对稿；仍有编辑提交方式和视觉样式差异，未宣称 1:1 复刻。
