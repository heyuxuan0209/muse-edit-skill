# 原版课程编辑器与 Skill 一致性排查（2026-09-10）

对照基准：原版 `课程模式编辑器-方案mock.html` 的实际有效代码，以及 9 月 3—4 日交接记录中的最终产品决定。不能只凭旧函数名判断功能：三色高亮工具栏已被用户取消；旧的音频富排版复制也已被轻量播客简介取代。

结论：当前 Skill 是独立实现，不能称为原版 1:1 封装。本次补齐功能遗漏，并明确保留尚未一致的交互与视觉差异。

## 本次发现并修复

| 项目 | 排查前问题 | 现在的行为 |
|---|---|---|
| 导入后的恢复基准 | 恢复仍依赖最初构建版本，不同课程不能恢复；同 id 导入可能恢复成旧内容 | 保存本次导入的建议基准，刷新后仍恢复该基准 |
| 模块完整恢复 | 未恢复概念项、下一课锚点；关闭模块恢复后可能仍关闭 | 一并恢复相关字段，重新启用；无有效锚点的空草稿明确提示 |
| 推荐形式恢复 | 缺少原版的一键恢复入口；quote 模块恢复不恢复规则 | 恢复每条 recommended，重新启用整组；冲突时保留当前状态并提示 |
| 推荐信息 | 未展示推荐形式；整组关闭后仍像在正常生效 | 展示推荐形式和整组关闭说明 |
| 结构地图 | 关闭模块从地图消失，无法从地图找回 | 地图保留已移除模块和编辑入口 |
| 章节跳转 | 从播客点击章节仍留在播客页 | 切回图文并定位、高亮对应章节 |
| 组件联动回归 | 重建表单可能让展开状态抢走播客预览 | 保留展开状态，初始化事件不抢焦点 |
| 听音入口 | 听音卡没有跳转，也缺少时长、原生音频插入提示 | 卡片可跳转音频，发布 HTML 提醒插入平台原生音频 |
| 发布确认 | 只改按钮文字，未呈现发布检查流程 | 显示步骤状态、检查清单、复制入口；刷新恢复，编辑后撤销确认 |
| 播客内容 | 简介缺少时长和听读路径，节目笔记页没有封面 | 简介补齐现有课程信息；节目笔记封面随替换同步 |
| 校对稿 | 只有原稿，没有独立章节标题清单 | 浏览器和 CLI 都输出章节清单与完整原稿，不生成虚构时间码 |
| 练习内容 | 没有 nextLesson 锚点时正文第二行以后被丢弃 | 剩余文案保留在练习卡 |
| 辅助操作 | 原稿回到开头、封面遮罩点击关闭缺失 | 两项均补齐 |

## 仍然不一致，不能声称已经复刻

| 项目 | 原版 | 当前 Skill / 本轮处理 |
|---|---|---|
| 编辑提交方式 | 编辑临时草稿，点击“应用并折叠”，可取消 | 实时预览并自动保存，没有相同的取消本次编辑动作。本轮没有更换整个编辑状态模型；恢复建议不是撤销上一次编辑 |
| 视觉模板 | 听读路径列表、不同阅读卡配色和形状、深色播客外观、品牌细节 | 共用较简化的卡片样式和原生音频控件；并非像素级一致，公众号复制后的排版也不是完全相同 |
| 拉丁字母间距 | 原版合并连续大写字母间空格，例如 A C → AC | 当前主要处理中文标点，未沿用此规则；需要评估避免把有意分隔的英文误合并 |
| 封面上传限制 | 12 MB，PNG/JPG/WebP | 8 MB，另支持 GIF；存储失败时明确要求导出备份。限制未统一 |

这些差异不是用户已同意的取舍；这里记录实际状态，避免把独立实现说成原版直接包装。

## 已核对且不应倒退的行为

- 导入、导出课程 JSON；非法导入不覆盖当前课程。音频不会被 JSON 内嵌，素材相对路径仍需与发布目录对应；迁移完整发布材料使用 CLI build。
- 原稿只读；新增阅读层可关；AI 重点和自定义重点独立；引用只出现一次。
- 图文标题、副标题、正文独立复制；正文采用内联样式，不包含交互按钮、定位标记和标题栏。
- 播客发布轻量节目笔记，音频单独上传；旧的“复制音频正文”实际已改为简介，不恢复无效富排版路线。
- 不恢复已被用户否定的三色高亮；不把原版播放器上的静态转写、评论、时间轴、列表装饰当作真实功能。
- 本机草稿按页面与构建保存；存储满会报错；导出文件才是可迁移备份。旧 mock 的“已保存到本机”文案不能作为真实保存证据。
- 现有页面升级保留内嵌课程、构建 id 与浏览器草稿键；`article.html` 仍是构建时快照，最新改动需通过页面下载或重新构建。

## 验证证据

- 12 项 Node 测试通过：包含原稿对比、输入校验、引用不重复、CLI 构建、空下一课锚点的练习全文、节目笔记和章节清单。
- 完整浏览器回归通过：真实剪贴板、导出再导入、非法输入不覆盖、导入新课后修改/刷新/恢复、关闭模块地图入口、推荐恢复、确认/刷新/再编辑、听音跳转、封面上传/放大/下载、存储失败、390 px 无横向溢出，页面异常为 0。
- 总览＋第 1—6 课重新构建并通过素材严格检查及公众号 HTML 校验，七份均 0 ERROR / 0 WARNING。
- 浏览器逐一核对七课，共 304 个正文/章节源行；左栏与课程 JSON 原稿一致，右栏逐行等于声明的显示转换，七课音频均可读时长，两类封面均可解码，节目笔记页封面可加载。
- 上述检查不包含微信平台当前版本的实际粘贴及手机二次渲染验收。
